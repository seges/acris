#summary Available Annotation Processing profiles for project quick-start.
#labels Project,QuickStart,PAP

= SeSAM Annotation Processing profile =

|| *Profile* || *Activation* || *Purpose* ||
|| pap || file actiobation - .pap || annotation processor configuration for maven and eclpise ||  
|| selenium || file activation - .selenium || selenium based project - with all selenium dependencies ||
|| generate-site || property activation - -DgenerateSite=true || generates documentation before release ||
|| selenium-api || file activation - .selenium-api || *Deprecated* ||